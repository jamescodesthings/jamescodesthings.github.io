# UX Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **After each task completes:** mark the task's steps `[x]` in this plan. Commit both updates with the task's final commit.
>
> **Push rule:** Every `git commit` must be immediately followed by `git push origin claude-cleanup`. Never leave unpushed commits.
>
> **Browser testing:** The `browse` skill is available globally as a persistent headless Chromium browser. Use it for ALL visual verification steps. Pattern: `npm start && npm run server &`, then `$B goto http://localhost:8080`, `$B screenshot /tmp/check.png`, Read the screenshot. Always kill the server after: `pkill -f "node src/server"`.

**Goal:** Reorder page sections so CV content comes last, add a persistent Campsnap FAB with video popover, fix the footer logo sizing, and add an amber background to the favicon.

**Architecture:** Pure template/CSS/JS changes to the existing EJS static site generator. No new dependencies. The FAB is a new `campsnap-fab.ejs` partial + `fab.js` vanilla JS module. All other changes are edits to existing files.

**Tech Stack:** EJS templates, vanilla CSS custom properties, vanilla JS (ES module IIFE pattern matching `theme.js`), Node.js 22 build pipeline.

---

## File Structure

| File | Change |
|------|--------|
| `src/templates/index.ejs` | Reorder section includes, add FAB include + fab.js script tag, renumber blog label |
| `src/templates/sections/campsnap-fab.ejs` | **New** — FAB button + popover markup |
| `src/js/fab.js` | **New** — FAB open/close logic |
| `src/css/styles.css` | FAB/popover CSS, footer logo fix |
| `src/templates/sections/cover-letter.ejs` | Add `03 — ABOUT` section label |
| `src/templates/sections/experience.ejs` | Renumber label to `04 — EXPERIENCE` |
| `src/templates/sections/skills.ejs` | Renumber label to `05 — SKILLS & EDUCATION` |
| `src/templates/sections/projects.ejs` | Renumber label to `06 — PROJECTS` |
| `src/templates/sections/footer.ejs` | Logo `width` attribute `120` → `160` |
| `src/assets/icons/favicon.svg` | Add amber background rectangle |
| `src/assets/icons/favicon.ico` | Regenerated from new SVG |
| `src/assets/icons/apple-touch-icon.png` | Regenerated from new SVG |
| `src/assets/icons/icon-192.png` | Regenerated from new SVG |
| `src/assets/icons/icon-512.png` | Regenerated from new SVG |

---

## Task 1: Section Reorder + Renumber Labels

**Files:**
- Modify: `src/templates/index.ejs`
- Modify: `src/templates/sections/cover-letter.ejs`
- Modify: `src/templates/sections/experience.ejs`
- Modify: `src/templates/sections/skills.ejs`
- Modify: `src/templates/sections/projects.ejs`

- [ ] **Step 1: Reorder sections in index.ejs**

Replace the section includes block (lines 37–61). New order: hero → blog → campsnap-banner → socials → cover-letter → experience → skills → projects → footer.

The complete new body of `index.ejs` from the `<body>` open to `</body>`:

```html
<body>
  <button class="theme-toggle" id="darkModeToggle" aria-label="Toggle theme">
    <span class="toggle-icon" id="toggleIcon">
      <svg class="icon-sun" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/></svg>
      <svg class="icon-moon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
    </span>
  </button>
  <%- include('sections/hero', { name: 'James Macmillan' }) %>
  <% if (blogPosts && blogPosts.length > 0) { %>
  <section class="section" data-animate>
    <div class="section-inner">
      <span class="section-label">01 — BLOG</span>
      <h2 class="section-title">Writing</h2>
      <div class="blog-cards">
        <% blogPosts.forEach(function(post) { %>
        <a href="blog/<%= post.slug %>.html" class="blog-card">
          <span class="blog-card-title"><%= post.title %></span>
          <% if (post.date) { %><span class="blog-card-date"><%= formatDate(post.date) %></span><% } %>
          <svg class="blog-card-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </a>
        <% }); %>
      </div>
    </div>
  </section>
  <% } %>
  <%- include('sections/campsnap-banner') %>
  <%- include('sections/socials', { socials }) %>
  <%- include('sections/cover-letter', { sidebar, coverLetter, profile }) %>
  <%- include('sections/experience', { sidebar, experience, profile }) %>
  <%- include('sections/skills', { sidebar, skills, education }) %>
  <%- include('sections/projects', { sidebar, projects }) %>
  <%- include('sections/campsnap-fab') %>
  <%- include('sections/footer') %>

  <script src="js/theme.js"></script>
  <script src="js/fab.js"></script>
</body>
```

Note: `campsnap-fab` and `fab.js` are added here — they don't exist yet (Task 2 creates them). The build will still succeed because EJS `include` errors will be caught in Task 2. Actually — create a placeholder for now to avoid build errors:

```bash
echo '<div></div>' > /Users/jamesmacmillan/projects/personal/codesthings/src/templates/sections/campsnap-fab.ejs
echo '' > /Users/jamesmacmillan/projects/personal/codesthings/src/js/fab.js
```

- [ ] **Step 2: Add section label to cover-letter.ejs**

In `src/templates/sections/cover-letter.ejs`, insert the section label after `<div class="section-inner">` (line 2):

Change:
```html
<section class="section" data-animate>
  <div class="section-inner">
    <div class="about-grid">
```

To:
```html
<section class="section" data-animate>
  <div class="section-inner">
    <span class="section-label">03 — ABOUT</span>
    <div class="about-grid">
```

- [ ] **Step 3: Renumber experience.ejs**

In `src/templates/sections/experience.ejs`, change:
```html
<span class="section-label">02 — EXPERIENCE</span>
```
To:
```html
<span class="section-label">04 — EXPERIENCE</span>
```

- [ ] **Step 4: Renumber skills.ejs**

In `src/templates/sections/skills.ejs`, change:
```html
<span class="section-label">03 — SKILLS</span>
```
To:
```html
<span class="section-label">05 — SKILLS & EDUCATION</span>
```

- [ ] **Step 5: Renumber projects.ejs**

In `src/templates/sections/projects.ejs`, change:
```html
<span class="section-label">05 — PROJECTS</span>
```
To:
```html
<span class="section-label">06 — PROJECTS</span>
```

- [ ] **Step 6: Build and verify order**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings && npm start 2>&1 | tail -5
```
Expected: `Build complete!`

Start server, take screenshot, check section order visually:
```bash
npm run server &
```
```
$B goto http://localhost:8080
$B js "document.querySelectorAll('[data-animate]').forEach(el => el.classList.add('animate-in'))"
$B js "window.scrollBy(0, 900)"
$B screenshot --viewport /tmp/task1-check.png
```
Read `/tmp/task1-check.png`. The first section after the hero should be the blog section.
```bash
pkill -f "node src/server"
```

- [ ] **Step 7: Commit**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings
git add src/templates/index.ejs src/templates/sections/cover-letter.ejs src/templates/sections/experience.ejs src/templates/sections/skills.ejs src/templates/sections/projects.ejs src/templates/sections/campsnap-fab.ejs src/js/fab.js
git commit -m "feat: reorder page sections — blog first, CV last

New order: hero → blog → campsnap → socials → about → experience → skills → projects

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
git push origin claude-cleanup
```

---

## Task 2: Footer Logo Fix

**Files:**
- Modify: `src/templates/sections/footer.ejs`
- Modify: `src/css/styles.css` (`.footer-logo` rule at line ~901)

- [ ] **Step 1: Update footer.ejs logo width**

In `src/templates/sections/footer.ejs`, change both `width="120"` attributes to `width="160"`:

```html
<footer class="site-footer">
  <div class="footer-inner">
    <img src="assets/logo/logo.svg" alt="codesthings" class="footer-logo footer-logo--light" width="160" loading="lazy">
    <img src="assets/logo/logo-dark.svg" alt="" class="footer-logo footer-logo--dark" width="160" loading="lazy" aria-hidden="true">
    <div class="footer-links">
      <a href="cv.pdf" class="footer-link" download>Download CV</a>
    </div>
    <span class="footer-copy">&copy; 2026 James Macmillan</span>
  </div>
</footer>
```

- [ ] **Step 2: Fix footer logo CSS**

In `src/css/styles.css`, the current `.footer-logo` rule (around line 901) is:
```css
.footer-logo {
  height: 28px;
  width: auto;
  opacity: 0.85;
}
```

Change it to:
```css
.footer-logo {
  max-width: 160px;
  height: auto;
  opacity: 0.85;
}
```

This removes the fixed height constraint that was squishing the logo, and lets it scale proportionally up to 160px wide.

- [ ] **Step 3: Build and verify**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings && npm start 2>&1 | tail -3
npm run server &
```
```
$B goto http://localhost:8080
$B js "window.scrollTo(0, document.body.scrollHeight)"
$B screenshot --viewport /tmp/footer-check.png
```
Read `/tmp/footer-check.png`. The footer logo should be wider and not squished.
```bash
pkill -f "node src/server"
```

- [ ] **Step 4: Commit**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings
git add src/templates/sections/footer.ejs src/css/styles.css
git commit -m "fix: widen footer logo area, remove constraining height

max-width: 160px + height: auto preserves aspect ratio.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
git push origin claude-cleanup
```

---

## Task 3: Favicon Background

**Files:**
- Modify: `src/assets/icons/favicon.svg`
- Modify: `src/assets/icons/favicon.ico` (regenerated)
- Modify: `src/assets/icons/apple-touch-icon.png` (regenerated)
- Modify: `src/assets/icons/icon-192.png` (regenerated)
- Modify: `src/assets/icons/icon-512.png` (regenerated)

- [ ] **Step 1: Update favicon.svg with amber background**

Replace `src/assets/icons/favicon.svg` with this content (adds amber `#f59e0b` filled background rect, darkens the dot to `#0a0a0f` for contrast on amber):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<svg id="logo" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
  <defs>
    <style>
      .cls-bg {
        fill: #f59e0b;
      }
      .cls-1 {
        fill: #0a0a0f;
      }
      .cls-2 {
        fill: #0a0a0f;
      }
    </style>
  </defs>
  <rect class="cls-bg" width="128" height="128"/>
  <g id="logo-word">
    <path class="cls-1" d="M30.54,64c0-34.57,7.36-44.85,31.8-44.85,12.5,0,18.33,2.78,25.13,12.5l-3.47,2.08c-5.83-8.05-11.39-10.69-21.66-10.69-20.97,0-27.63,9.72-27.63,40.96s6.53,40.96,27.08,40.96c10.42,0,16.11-2.78,22.22-10.83l3.47,2.08c-6.94,9.72-13.05,12.64-25.69,12.64-24.02,0-31.24-10.27-31.24-44.85Z"/>
    <path id="dot-com" class="cls-2" d="M70.84,63.83c0-12.63,3.66-15.95,16.62-15.95s16.62,3.32,16.62,15.95-3.66,16.29-16.62,16.29-16.62-3.66-16.62-16.29Z"/>
  </g>
</svg>
```

- [ ] **Step 2: Regenerate raster favicon files**

Check what tools are available:
```bash
which convert && echo "imagemagick ok" || echo "no imagemagick"
which rsvg-convert && echo "rsvg ok" || echo "no rsvg"
```

If ImageMagick is available (`convert`):
```bash
cd /Users/jamesmacmillan/projects/personal/codesthings/src/assets/icons

# apple-touch-icon (180x180)
convert -background none -resize 180x180 favicon.svg apple-touch-icon.png

# icon-192
convert -background none -resize 192x192 favicon.svg icon-192.png

# icon-512
convert -background none -resize 512x512 favicon.svg icon-512.png

# favicon.ico (multi-size: 16, 32, 48)
convert -background none \
  \( favicon.svg -resize 16x16 \) \
  \( favicon.svg -resize 32x32 \) \
  \( favicon.svg -resize 48x48 \) \
  favicon.ico
```

If `convert` is not available, install via Homebrew:
```bash
brew install imagemagick
```
Then run the commands above.

- [ ] **Step 3: Verify favicon looks right**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings && npm start 2>&1 | tail -3
npm run server &
```
```
$B goto http://localhost:8080
$B screenshot /tmp/favicon-check.png
```
Read `/tmp/favicon-check.png` — in the browser address bar/tab area the favicon should show as an amber square with a dark C. If the browser cached the old favicon, do:
```
$B js "window.location.reload(true)"
$B screenshot /tmp/favicon-check2.png
```
```bash
pkill -f "node src/server"
```

- [ ] **Step 4: Commit**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings
git add src/assets/icons/favicon.svg src/assets/icons/favicon.ico src/assets/icons/apple-touch-icon.png src/assets/icons/icon-192.png src/assets/icons/icon-512.png
git commit -m "feat: add amber background to favicon

Amber #f59e0b fill ensures the C mark is visible in all browser
tab chrome (both light and dark system themes).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
git push origin claude-cleanup
```

---

## Task 4: Campsnap FAB

**Files:**
- Modify: `src/templates/sections/campsnap-fab.ejs` (replace placeholder from Task 1)
- Modify: `src/js/fab.js` (replace placeholder from Task 1)
- Modify: `src/css/styles.css` (add FAB styles)

- [ ] **Step 1: Write campsnap-fab.ejs**

Replace the placeholder `src/templates/sections/campsnap-fab.ejs` with:

```html
<div class="campsnap-fab" id="campsnapFab">
  <div class="campsnap-fab-popover" id="campsnapPopover" aria-hidden="true">
    <button class="campsnap-fab-close" id="campsnapClose" aria-label="Close">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"/>
        <line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
    <video class="campsnap-fab-video" autoplay loop muted playsinline>
      <source src="assets/zipline.webm" type="video/webm" />
      <source src="assets/zipline.mp4" type="video/mp4" />
    </video>
    <p class="campsnap-fab-caption">I feel like you're just here for the campsnap filters.</p>
    <a href="https://codesthings.com/campsnap" class="campsnap-fab-link">
      Get the filters
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="5" y1="12" x2="19" y2="12"/>
        <polyline points="12 5 19 12 12 19"/>
      </svg>
    </a>
  </div>
  <button class="campsnap-fab-btn" id="campsnapFabBtn" aria-label="Campsnap filters" aria-expanded="false">
    <svg class="campsnap-fab-icon" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/>
      <path d="M20 3v4"/>
      <path d="M22 5h-4"/>
    </svg>
    <span class="campsnap-fab-label">campsnap</span>
  </button>
</div>
```

- [ ] **Step 2: Write fab.js**

Replace the placeholder `src/js/fab.js` with:

```js
(function () {
  var fab = document.getElementById('campsnapFab');
  var btn = document.getElementById('campsnapFabBtn');
  var popover = document.getElementById('campsnapPopover');
  var closeBtn = document.getElementById('campsnapClose');

  if (!fab || !btn || !popover || !closeBtn) return;

  function openPopover() {
    popover.classList.add('is-open');
    btn.setAttribute('aria-expanded', 'true');
    popover.removeAttribute('aria-hidden');
  }

  function closePopover() {
    popover.classList.remove('is-open');
    btn.setAttribute('aria-expanded', 'false');
    popover.setAttribute('aria-hidden', 'true');
  }

  btn.addEventListener('click', function (e) {
    e.stopPropagation();
    popover.classList.contains('is-open') ? closePopover() : openPopover();
  });

  closeBtn.addEventListener('click', function (e) {
    e.stopPropagation();
    closePopover();
  });

  document.addEventListener('click', function (e) {
    if (!fab.contains(e.target)) {
      closePopover();
    }
  });

  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') {
      closePopover();
    }
  });
})();
```

- [ ] **Step 3: Add FAB CSS to styles.css**

Find the `@media print` block in `src/css/styles.css` (search for `@media print`). Before that block, add the following CSS. Add it after the existing `.campsnap-*` styles (search for `.campsnap-banner` to find the right area):

```css
/* ============================================
   Campsnap FAB
   ============================================ */

.campsnap-fab {
  position: fixed;
  bottom: var(--space-xl);
  right: var(--space-xl);
  z-index: 90;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: var(--space-sm);
}

.campsnap-fab-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  width: 56px;
  padding: var(--space-sm) 0;
  background: var(--color-accent);
  border: none;
  border-radius: 16px;
  cursor: pointer;
  color: #0a0a0f;
  transition: transform 0.15s ease, box-shadow 0.15s ease;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
}

.campsnap-fab-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
}

.campsnap-fab-label {
  font-family: var(--font-mono);
  font-size: 9px;
  font-weight: 500;
  letter-spacing: 0.05em;
  text-transform: lowercase;
  color: #0a0a0f;
}

.campsnap-fab-popover {
  display: none;
  flex-direction: column;
  gap: var(--space-md);
  width: 280px;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--border-radius);
  overflow: hidden;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  position: relative;
}

.campsnap-fab-popover.is-open {
  display: flex;
}

.campsnap-fab-close {
  position: absolute;
  top: var(--space-sm);
  right: var(--space-sm);
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.4);
  border: none;
  border-radius: 50%;
  cursor: pointer;
  color: var(--color-text);
  z-index: 1;
  transition: background 0.15s ease;
}

.campsnap-fab-close:hover {
  background: rgba(0, 0, 0, 0.6);
}

.campsnap-fab-video {
  width: 100%;
  display: block;
  border-radius: 0;
}

.campsnap-fab-caption {
  font-size: var(--text-sm);
  color: var(--color-text);
  padding: 0 var(--space-md);
  margin: 0;
  line-height: 1.5;
}

.campsnap-fab-link {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-sm) var(--space-md) var(--space-md);
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  font-weight: 500;
  color: var(--color-accent);
  text-decoration: none;
  letter-spacing: 0.03em;
  transition: color 0.15s ease;
}

.campsnap-fab-link:hover {
  color: var(--color-heading);
}

@media print {
  .campsnap-fab {
    display: none;
  }
}
```

- [ ] **Step 4: Build and verify**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings && npm start 2>&1 | tail -5
```
Expected: `Build complete!`

```bash
npm run server &
```
```
$B goto http://localhost:8080
$B screenshot --viewport /tmp/fab-closed.png
```
Read `/tmp/fab-closed.png` — should show an amber pill button in the bottom-right corner.

Click the FAB:
```
$B click "#campsnapFabBtn"
$B screenshot --viewport /tmp/fab-open.png
```
Read `/tmp/fab-open.png` — popover should appear above the FAB with the video playing, caption text, and "Get the filters" link.

Close it with Escape:
```
$B press Escape
$B screenshot --viewport /tmp/fab-closed2.png
```
Read `/tmp/fab-closed2.png` — popover should be gone.

```bash
pkill -f "node src/server"
```

- [ ] **Step 5: Run format check and fix**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings && npm run format:fix
```

- [ ] **Step 6: Commit**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings
git add src/templates/sections/campsnap-fab.ejs src/js/fab.js src/css/styles.css
git commit -m "feat: add campsnap FAB with video popover

Persistent bottom-right FAB opens a popover with the zipline
video + link to filters. Escape/outside-click closes it.
Hidden on print.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
git push origin claude-cleanup
```

---

## Task 5: Final Verification + Format Check

- [ ] **Step 1: Full build**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings && npm start 2>&1 | tail -5
```
Expected: `Build complete!`

- [ ] **Step 2: Format check**

```bash
npm run format:check
```
Expected: `All matched files use Prettier code style!`

- [ ] **Step 3: Visual check — full page**

```bash
npm run server &
```
```
$B goto http://localhost:8080
$B js "document.querySelectorAll('[data-animate]').forEach(el => el.classList.add('animate-in'))"
$B js "window.scrollBy(0, 900)"
$B screenshot --viewport /tmp/final-top.png
```
Read `/tmp/final-top.png` — first section below hero should be blog.

```
$B js "window.scrollTo(0, document.body.scrollHeight)"
$B screenshot --viewport /tmp/final-bottom.png
```
Read `/tmp/final-bottom.png` — footer logo should be wider, FAB visible bottom-right.

```bash
pkill -f "node src/server"
```

- [ ] **Step 4: Final commit and push**

```bash
cd /Users/jamesmacmillan/projects/personal/codesthings
git add docs/superpowers/plans/2026-04-12-ux-polish.md
git commit -m "chore: mark UX polish plan complete"
git push origin claude-cleanup
```
