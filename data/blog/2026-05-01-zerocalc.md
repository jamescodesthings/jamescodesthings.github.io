# ZeroCalc

_Published: 01 May 2026_

How to go from delivery to a ZeroCalc 2W in 2026: My happy standalone little Pico8 dev machine.

## What to expect

Here's a quick demo of Pico8 running on my Zerocalc 2W.

<iframe class="yt" src="https://www.youtube-nocookie.com/embed/A72Jcj_3LAM?si=t_zLvrG_AkeEjOAf" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
_Ignore the 3d printer sounds, I'm printing a handle for the thing._

## Why write this?

There's a bunch of awesome people over at [forum.clockworkpi.com](https://forum.clockworkpi.com/) who have worked out how to make this work. But, the forum post is a bit difficult to navigate, and find out what you absolutely need, vs. what's just cool tinkering.

**For my purposes;** the PicoCalc hits a great form factor and price point for a standalone [Pico8](https://www.lexaloffle.com/pico-8.php) dev machine. I love the [Pico8 platform](https://www.lexaloffle.com/pico-8.php) and regularly tinker with small game ideas.

I've previously used a [uconsole](https://www.clockworkpi.com/uconsole) with a RPI CM5 for this purposes; but it's overkill.

The Pi Pico that the PicoCalc uses by default lacks power to run Pico8 well, like I'd like it to.

My aim was to get the PicoCalc to run on a Zero 2W with `pico8` and `git`. So that I can tinker with ideas, and hand them off to a repo and pick up elsewhere.

I don't need `external USB`/`gpio`/`etc`.

Just `pico8`, `a display`, `a keyboard`, `audio`, and `internet`.

_If your needs are different, don't worry!_ I've left links to all the resources and hints about where to look for different setups throughout this guide. The Zero2W is fairly capable compute with a lot of versatility, you could use it for all sorts.

# What you need

1. [A PicoCalc kit](https://www.clockworkpi.com/picocalc) - I got mine from AliExpress.
1. [A Raspberry Pi Zero 2WH](https://www.raspberrypi.com/products/raspberry-pi-zero-2-w/) - The WH stands for Wifi + Headers (presoldered headers). You can also just get a 2W and solder the headers yourself.
1. [A reasonably speedy SD card with a bit of space (32gb+)](https://www.amazon.co.uk/dp/B0B7NTY2S6?ref=ppx_yo2ov_dt_b_fed_asin_title) - I use Sandisk Ultras because I trust them, I wanted about 128GB so this is what I went with.
1. [The adapter board](https://www.pcbway.com/project/shareproject/Picocalc_Zero_Mod_for_Raspberry_Pi_Zero_2W_1c0c1a06.html) - I purchased from PCBWay, they sold me 5 for approx $1us each. Shipped to the UK in 3-4 days for a reasonable cost ($20us).
1. [2x20 pin headers with 2.54mm pitch](https://www.amazon.co.uk/dp/B0DXKP8Y8S?ref=ppx_yo2ov_dt_b_fed_asin_title) - I bought mine from amazon in a set, I only used 2 of the 20pin headers. My Pi Zero came with headers presoldered. If you get the Pi zero without headers you'll need some more for that too.
1. [2x 18650 batteries from a good source](https://www.nubattery.co.uk/panasonic-ncr18650b-18650-battery) - I bought mine (flat top) from NuBattery in the UK. There's [forum posts on battery options](https://forum.clockworkpi.com/t/picocalc-18650-battery-advice/16209/6) if you're stuck.
1. A trusty soldering iron and some solder.

## Optional: soldering helpers

I also used 2 female 8 pin headers (2.54mm pitch) to help with soldering. Mine came from [this kit](https://www.amazon.co.uk/dp/B095Y8L12Z?ref=ppx_yo2ov_dt_b_fed_asin_title) some time ago.

_tldr;_ if you plug the male headers into the female and sit the adapter board on top it's super easy to solder them straight.

[![Like this](/assets/blog-images/solder-setup.png)](/assets/blog-images/solder-setup.png)

## Optional: Case

There are other optional features like external usb, and a custom case. Most of the details of those can be found
on [Michael's Printables project](https://www.printables.com/model/1399739-picocalc-zero-mod-case).

# Flashing the SD Card

Source: [github.com/nibheis/picocalc_trixie: Step 1](https://github.com/nibheis/picocalc_trixie#step-1-install-raspberry-pi-os-lite-64-bit-debian-trixie-using-the-raspberry-pi-imager)

1. Download and install [Raspberry Pi Imager](https://www.raspberrypi.com/software/)
1. Plug in your SD card
1. Open Raspberry Pi Imager

In Imager:

```
1. On the select device page choose "Raspberry Pi Zero 2 W" and hit next
2. On the Choose OS page, choose "Raspberry Pi OS (other)" and hit next
3. On the next page, choose "Raspberry Pi OS Lite (64 Bit)" (A port of Debian Trixie) and hit next
4. On the storage page, select your SD card and hit next
5. On the Customization page, set your hostname. I used "zerocalc" for mine, but you can choose whatever you like.
6. On the Localisation page, set your locale. But! Set your keyboard layout to "us".
  - If you get this wrong, like I did, it can be fixed later.
  - You'll know you got it wrong if you hit left shift + " but get an @.
7. On the User page, set your username, and password.
8. On the wifi page, set up your wifi credentials; so we can ssh for the rest of the install.
9. On the remote access page, enable SSH and set your authentication method as you wish.
10. Hit "Write" and wait for it to finish.
11. Unplug the SD card and plug it into your Pi Zero 2W.
```

## 3 Building the hardware

Others have covered this topic better than I could. You can watch Michael's video for a great walkthrough, and the summary below is the steps I took.

### Video

Michael posted a great build video that [goes over the soldering](https://www.youtube.com/watch?v=wgE0R1M0_ZE), and physical build.

### Text Summary

```
1. Solder the 20pin headers to the adapter board
  - on the back, or the "other side" of the text that says "Header on the other side"
2. Solder the Pi to the adapter board as well
  - The headers stick through the same way
3. Plug in your micro sd to the Pi Zero 2w (Flash instructions below)
4. Plug the adapter board into your PicoCalc
5. Add batteries
6. Screw everything together
```

[![It should look like this](/assets/blog-images/after-soldering.png)](assets/blog-images/after-soldering.png)

_Note: in this image my SD card isn't plugged in, it should be plugged into the Pi Zero 2W._

## Software setup

Source: [github.com/nibheis/picocalc_trixie](https://github.com/nibheis/picocalc_trixie)

### Power on.

Once you have flashed the SD card and completed the hardware build. Power it on. You'll find it may reboot a couple times the first time you hit it; don't worry, it'll eventually come up stable. I think it's doing SD expansiona nd initial set up at that time.

### Find your Pi on the network

The easiest wat to find your zerocalc on the network is to use `ping` with the hostname you set in the imager. For example, if you set your hostname to `zerocalc` you can ping it with:

```
ping zerocalc.local
# or
ping zerocalc
```

### SSH into the Pi

Use the same hostname or ip to ssh in:

```
ssh zerocalc
# You can prepend your username to set the correct user to log in to as well.
ssh james@zerocalc
```

### Update installed software

Out the gate you want to run apt update.

```
sudo apt update && sudo apt upgrade -y
```

### Install git

```
sudo apt install git -y
```

### Clone the picocalc_trixie repo

```
git clone https://github.com/nibheis/picocalc_trixie.git
```

It should be cloned into `~/picocalc_trixie`(`/home/your-username/picocalc_trixie`)

### In the picocalc_trixie directory, copy the display firmware

```
cd picocalc_trixie
sudo cp display/picomipi.bin /lib/firmware/
```

## Display setup

### Activate the SPI Interface for the display

```
sudo nano /boot/firmware/config.txt
```

Add the following lines to the end of the file:

```
# Enable SPI for the display
dtparam=spi=on

# Driver configuration for the display
dtoverlay=mipi-dbi-spi,spi0-0,speed=70000000
dtparam=compatible=picomipi\0panel-mipi-dbi-spi
dtparam=width=320,height=320,width-mm=43,height-mm=43
dtparam=reset-gpio=25,dc-gpio=24
dtparam=backlight-gpio=18
dtparam=clock-frequency=50
```

### configure the framebuffer configuration in the kernel command line

```
sudo nano /boot/firmware/cmdline.txt
```

Add the following to the end of the line:

```
fbcon=map:1 fbcon=font:MINI4x6
```

### Reboot

```
sudo reboot
```

When you reboot this time, your display should work, but your keyboard won't be set up yet.

## Set up the keyboard

### Navigate to the keyboard firmware directory

1. SSH back into the Pi
1. `cd` to the `picocalc_trixie` directory
1. `cd` into the `keyboard` directory

#### If you need the mouse, edit the firmware

If you need the mouse emulation that comes with pico calc:
`press shift to turn the arrow keys into the mouse, and the square brackets into lmb and rmb`

You need to update the `picocalc_kbd.c` file to uncomment the disabled mouse emulation.

```
sudo nano picocalc_kbd/picocalc_kbd.c
```

- Remove the `//|` in front of the lines commented out.
- They are the only lines commented out with this pattern `//|` so it should be easy to find.
- [At the time of writing it's lines 223-311](https://github.com/nibheis/picocalc_trixie/blob/dccf9c22fa913b1abc67eb5e682ed41a95d40792/keyboard/picocalc_kbd/picocalc_kbd.c)
- Delete the `//|` from the start of each line in that block, and save the file.

### Build and install the keyboard firmware

```
# In the picocalc_trixie/keyboard directory
chmod +x setup_keyboard.sh
sudo ./setup_keyboard.sh
```

### Confirm the keyboard setup worked

```
sudo cat /boot/firmware/config.txt
```

The start of the file should now have the following lines:

```
dtparam=i2c_arm=on
dtoverlay=picocalc_kbd
```

## Configure Audio

```
sudo nano /boot/firmware/config.txt
```

Add the following lines to the end of the file:

```
dtparam=audio=on
dtoverlay=audremap,pins_12_13
```

Note, you may already see the `dtparam=audio=on` line in your config.txt, if so just add the `dtoverlay=audremap,pins_12_13` line.

### Reboot

```
sudo reboot
```

Once you've rebooted you should have most things working! The rest is optional but might be handy.

## optional: Configure your terminal font for a better display experience

Source: [a comment by Lachlan_Beveridge](https://forum.clockworkpi.com/t/raspberry-pi-zero-2-on-picocalc/17946/235?u=jamescodesthings)

```
sudo dpkg-reconfigure console-setup
```

When you run this command, you'll be prompted with a series of questions. Here's how to answer them for the best experience on the PicoCalc display:

```
encoding: UTF-8
character set: Guess optimal
font: Terminus
size: 6×12
```

## optional: fix keyboard layout

if, like me you flashed with the wrong keyboard layout, you can fix it with:

```
sudo nano /etc/default/keyboard
```

1. Find the line that starts with `XKBLAYOUT=`
2. Change the value to `us` (or your desired layout)
3. Save the file and exit
4. Reboot the Pi

## Optional:

## Resources

Here's the main resources I used when working this out.

- [The forum post](https://forum.clockworkpi.com/t/raspberry-pi-zero-2-on-picocalc)
- [The hardware build video (from michael_mayer)](https://www.youtube.com/watch?v=wgE0R1M0_ZE)
- [The adapter board PCBWay project](https://www.pcbway.com/project/shareproject/Picocalc_Zero_Mod_for_Raspberry_Pi_Zero_2W_1c0c1a06.html)
- [The Trixie 64bit install instructions](https://github.com/nibheis/picocalc_trixie/)
- [A comment in the forum post about making the default font easier to work with](https://forum.clockworkpi.com/t/raspberry-pi-zero-2-on-picocalc/17946/235?u=jamescodesthings)

```

```
