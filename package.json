{
  "name": "codesthings",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier .",
    "format:check": "npm run format -- --check",
    "format:fix": "npm run format -- --write"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "2.8.2"
  }
}
