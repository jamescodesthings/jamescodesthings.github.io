<section class="section" data-animate>
  <div class="section-inner">
    <div class="about-grid">
      <div class="about-sidebar">
        <div class="avatar">
          <img src="assets/profile.png" alt="James Macmillan" class="avatar-img" loading="lazy">
        </div>
        <div class="contact-card">
          <h3 class="label">Contact</h3>
          <ul class="contact-list">
            <li>
              <span class="contact-label">Phone</span>
              <a href="tel:<%= sidebar.contact.phone %>"><%= sidebar.contact.phone %></a>
            </li>
            <li>
              <span class="contact-label">Email</span>
              <a href="mailto:<%= sidebar.contact.email %>"><%= sidebar.contact.email %></a>
            </li>
            <li>
              <span class="contact-label">Location</span>
              <span><%= sidebar.contact.address %></span>
            </li>
          </ul>
          <div class="contact-socials">
            <a href="<%= sidebar.socials.linkedin %>" target="_blank" rel="noopener noreferrer" class="social-link">LinkedIn</a>
            <a href="<%= sidebar.socials.whatsapp.url %>" target="_blank" rel="noopener noreferrer" class="social-link">WhatsApp</a>
          </div>
          <div class="availability">
            <span class="availability-dot"></span>
            <span><%= sidebar.availability %></span>
          </div>
        </div>
      </div>
      <div class="about-main">
        <span class="section-label">01 — ABOUT</span>
        <h2 class="section-title">Cover Letter</h2>
        <div class="prose">
          <% const content = coverLetter.seeking ? coverLetter.content : coverLetter.notSeekingContent; %>
          <% content.forEach(paragraph => { %>
            <p><%= paragraph %></p>
          <% }); %>
          <p class="signature"><%= coverLetter.signature %></p>
        </div>
      </div>
    </div>
  </div>
</section>
