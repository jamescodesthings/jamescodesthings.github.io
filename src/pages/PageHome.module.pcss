.theme-toolbar {
  @apply absolute w-auto top-4 right-4 z-1;
}

.main {
  @apply container mx-auto px-4;
}

.split-flap-wrapper {
  @apply sm:flex flex-wrap w-full sm:w-auto;
  @apply sm:p-0;
}
.jumbo-main-content {
  @apply flex flex-col justify-center items-center w-full h-full absolute top-0 left-0;
}
